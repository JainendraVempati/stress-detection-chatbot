# Git LFS removed — model files are handled differently:
# - stress_model.pt (254MB): downloaded at runtime via MODEL_DOWNLOAD_URL env var
# - tokenizer.pkl (833KB): committed directly to git (no LFS needed)
